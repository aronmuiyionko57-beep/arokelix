export const metadata = {
  title: "Start a Project | AROKELIX",
  description: "Tell AROKELIX about your project — websites, software, e-commerce, branding and more. We reply within one business day.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
export const metadata = {
  title: "Our Work | AROKELIX",
  description: "Real projects AROKELIX has delivered for real businesses, across websites, e-commerce and software.",
};

export default function WorkLayout({ children }: { children: React.ReactNode }) {
  return children;
}